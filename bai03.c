#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a;
	printf("nhap so a =");
	scanf("%d", &a);
	printf("Binh phuong cua so do la:%d", a*a);
	
	return 0;
}
